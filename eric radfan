<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eric Radfan | Portfolio</title>
    <meta name="description" content="Visual Designer & Artist - Portfolio von Eric Radfan">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;900&display=swap" rel="stylesheet">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --bg-dark: #0a0a0a;
            --bg-card: #161616;
            --text-primary: #ffffff;
            --text-secondary: rgba(255, 255, 255, 0.6);
            --accent: #ffffff;
            --border: rgba(255, 255, 255, 0.1);
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--bg-dark);
            color: var(--text-primary);
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* ==========================================
           SCROLL PROGRESS BAR
        ========================================== */
        .scroll-progress {
            position: fixed;
            top: 0;
            left: 0;
            height: 3px;
            background: linear-gradient(90deg, #fff, rgba(255,255,255,0.5));
            width: 0%;
            z-index: 10000;
            transition: width 0.1s ease;
        }

        /* ==========================================
           BACKGROUND
        ========================================== */
        .background {
            position: fixed;
            inset: 0;
            z-index: -1;
            background: linear-gradient(180deg, #0a0a0a 0%, #121212 100%);
        }

        .background::before {
            content: '';
            position: absolute;
            inset: 0;
            background-image: 
                radial-gradient(circle at 20% 20%, rgba(255,255,255,0.02) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(255,255,255,0.015) 0%, transparent 50%);
        }

        .background::after {
            content: '';
            position: absolute;
            inset: 0;
            background-image: 
                linear-gradient(rgba(255,255,255,0.01) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255,255,255,0.01) 1px, transparent 1px);
            background-size: 50px 50px;
        }

        /* ==========================================
           PARTICLES
        ========================================== */
        #particles {
            position: fixed;
            inset: 0;
            z-index: 0;
            pointer-events: none;
        }

        /* ==========================================
           NAVIGATION
        ========================================== */
        .nav-btn {
            position: fixed;
            top: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            background: rgba(255,255,255,0.05);
            border: 1px solid var(--border);
            border-radius: 50%;
            cursor: pointer;
            z-index: 1000;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 5px;
            transition: all 0.3s ease;
        }

        .nav-btn:hover {
            background: rgba(255,255,255,0.1);
            transform: scale(1.05);
        }

        .nav-btn span {
            width: 24px;
            height: 2px;
            background: white;
            transition: all 0.3s ease;
            border-radius: 2px;
        }

        .nav-btn.active span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
}

        .nav-btn.active span:nth-child(2) {
    opacity: 0;
}

        .nav-btn.active span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
}

        .nav-overlay {
            position: fixed;
            inset: 0;
            background: var(--bg-dark);
            z-index: 999;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transition: all 0.5s ease;
        }

        .nav-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .nav-menu {
            text-align: center;
        }

        .nav-menu > a,
        .nav-menu > div > a {
            display: inline-block;
            font-size: clamp(2rem, 8vw, 4rem);
            font-weight: 900;
            color: white;
            text-decoration: none;
            text-transform: uppercase;
            margin: 20px 0;
            transition: all 0.4s ease;
            letter-spacing: -0.02em;
        }

        .nav-menu > a:hover,
        .nav-menu > div > a:hover {
            opacity: 0.5;
        }

        /* Submenu styles */
        .nav-submenu {
            margin-top: 15px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
        }

        .nav-submenu a {
    font-size: clamp(1.2rem, 4vw, 1.8rem);
    font-weight: 600;
    margin: 8px 0;
    color: var(--text-secondary); /* Verwendet dein definiertes Grau */
    text-decoration: none;        /* Entfernt das Unterstreichen */
    opacity: 0.7;
    transition: all 0.3s ease;
}

        .nav-submenu a:hover {
    color: var(--text-primary); /* Wird beim Hover weiß */
    opacity: 1;
}

        /* ==========================================
           HERO SECTION
        ========================================== */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            position: relative;
            padding: 2rem;
        }

        .hero-label {
            font-size: 0.9rem;
            letter-spacing: 0.5em;
            text-transform: uppercase;
            opacity: 0.5;
            margin-bottom: 20px;
            font-weight: 300;
        }

        .hero h1 {
            font-size: clamp(3rem, 15vw, 12rem);
            font-weight: 900;
            letter-spacing: -0.05em;
            line-height: 0.9;
            margin-bottom: 30px;
            text-transform: uppercase;
        }

        .hero-subtitle {
            font-size: clamp(0.9rem, 2vw, 1.1rem);
            letter-spacing: 0.3em;
            text-transform: uppercase;
            opacity: 0.6;
            font-weight: 300;
        }

        .scroll-indicator {
            position: absolute;
            bottom: 50px;
            left: 50%;
            transform: translateX(-50%);
            opacity: 0.5;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateX(-50%) translateY(0); }
            50% { transform: translateX(-50%) translateY(-10px); }
        }

        /* ==========================================
           PROJECTS SECTION
        ========================================== */
        .projects {
            padding: 100px 2rem;
            max-width: 1600px;
            margin: 0 auto;
        }

        .section-title {
            font-size: 1.2rem;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 0.3em;
            text-align: center;
            margin-bottom: 40px;
            opacity: 0.8;
        }

        .project-section {
            margin-bottom: 100px;
        }

        .project-section:last-child {
            margin-bottom: 0;
        }

        /* SONGCOVER - 6 Squares (1:1) in 2 rows */
        .songcover-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-bottom: 100px;
        }

        /* THUMBNAILS - 1 big landscape (16:9) + 5 smaller landscapes */
        .thumbnails-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* 2 Spalten */
    grid-template-rows: repeat(3, 1fr);    /* 3 Zeilen */
    gap: 20px;
    margin-bottom: 100px;
}

.thumbnail-large {
    grid-column: auto;
    grid-row: auto;
}

        /* LOGO DESIGNS - 1 wide banner (1676x500) + 1 wide below */
        .logos-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 20px;
        }

        /* Box styles */
        .project-box {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            overflow: hidden;
            transition: all 0.4s ease;
            cursor: pointer;
            position: relative;
            aspect-ratio: 1/1;
        }

        .project-box.landscape {
            aspect-ratio: 16/9;
        }

        .project-box.banner {
            aspect-ratio: 1676/500;
        }

        .project-box:hover {
            transform: translateY(-8px);
            border-color: rgba(255,255,255,0.3);
            box-shadow: 0 20px 60px rgba(0,0,0,0.6);
        }

        .project-box img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.4s ease;
        }

        .project-box:hover img {
            transform: scale(1.05);
        }

        .project-info {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 25px;
            background: linear-gradient(to top, rgba(0,0,0,0.9), transparent);
            z-index: 2;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .project-box:hover .project-info {
            opacity: 1;
        }

        .project-info h3 {
            font-size: 1.2rem;
            font-weight: 900;
            text-transform: uppercase;
            margin: 0;
        }

        /* ==========================================
           LIGHTBOX (Image Popup)
        ========================================== */
        .lightbox {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.95);
            z-index: 10000;
            display: none;
            align-items: center;
            justify-content: center;
            cursor: zoom-out;
        }

        .lightbox.active {
            display: flex;
        }

        .lightbox img {
            max-width: 90%;
            max-height: 90%;
            object-fit: contain;
            border-radius: 8px;
            box-shadow: 0 0 100px rgba(0,0,0,0.8);
        }

        .lightbox-close {
    position: absolute;
    top: 30px;
    right: 30px;
    width: 50px;
    height: 50px;
    background: rgba(255,255,255,0.1);
    border: 1px solid rgba(255,255,255,0.2);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    font-size: 24px; /* Etwas kleiner für elegantere Optik */
    color: white;
    line-height: 1;  /* Verhindert vertikale Verschiebung */
    padding-bottom: 4px; /* Optische Korrektur, damit es mittig wirkt */
    transition: all 0.3s ease;
}

        .lightbox-close:hover {
            background: rgba(255,255,255,0.2);
            transform: rotate(90deg);
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .songcover-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .songcover-grid,
            .thumbnails-grid {
                grid-template-columns: 1fr;
            }

            .thumbnail-large {
                grid-column: span 1;
                grid-row: span 1;
            }

            .project-section {
                margin-bottom: 60px;
            }
        }

        /* ==========================================
           ABOUT & CONTACT
        ========================================== */
        .contact {
            padding: 200px 2rem 150px 2rem;
            text-align: center;
            max-width: 900px;
            margin: 0 auto;
        }

        .contact p {
            font-size: clamp(1rem, 2.5vw, 1.4rem);
            line-height: 1.7;
            margin-bottom: 60px;
            color: var(--text-secondary);
            font-weight: 300;
        }

        .contact-link {
            font-size: clamp(1.2rem, 4vw, 2rem);
            font-weight: 400;
            color: white;
            text-decoration: none;
            display: inline-block;
            padding: 15px 0;
            border-bottom: 1px solid transparent;
            transition: all 0.3s ease;
            letter-spacing: 0.02em;
        }

        .contact-link:hover {
            border-bottom-color: white;
            letter-spacing: 0.05em;
        }

        /* ==========================================
           RESPONSIVE
        ========================================== */
        @media (max-width: 768px) {
            .nav-btn {
                top: 20px;
                right: 20px;
                width: 50px;
                height: 50px;
            }
        }

        /* ==========================================
           UTILITY
        ========================================== */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

    .contact { 
    padding: 150px 2rem; 
    text-align: center; 
}

.email-btn {
    display: inline-block;
    padding: 18px 40px;
    border: 1px solid white; /* Weißer Rahmen */
    color: white;
    text-decoration: none;
    font-size: clamp(0.9rem, 3vw, 1.1rem);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.25em; /* Weiterer Buchstabenabstand für "High-End" Look */
    border-radius: 100px; /* Runde Kanten */
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    z-index: 1;
}

.email-btn:hover {
    color: black; /* Schrift wird schwarz beim Hover */
    background: white; /* Hintergrund wird weiß */
    transform: translateY(-5px); /* Kleiner Sprung nach oben */
    box-shadow: 0 10px 30px rgba(255,255,255,0.15); /* Subtiler Leuchteffekt */
}
    </style>
</head>
<body>

    <!-- Scroll Progress Bar -->
    <div class="scroll-progress"></div>

    <!-- Background -->
    <div class="background"></div>

    <!-- Particles Canvas -->
    <canvas id="particles"></canvas>

    <!-- Navigation Button -->
    <div class="nav-btn" id="navBtn">
        <span></span>
        <span></span>
        <span></span>
    </div>

    <!-- Navigation Overlay -->
    <div class="nav-overlay" id="navOverlay">
        <div class="nav-menu">
            <a href="#home">Home</a>
            <div>
                <a href="#work">Projekte</a>
                <div class="nav-submenu">
                    <a href="#songcover">SONGCOVER</a>
                    <a href="#thumbnails">THUMBNAILS</a>
                    <a href="#logodesigns">LOGO DESIGNS</a>
                </div>
            </div>
            <a href="#contact">Kontakt</a>
        </div>
    </div>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-label">Eric Radfan</div>
        <h1>PORTFOLIO</h1>
        <div class="hero-subtitle">Visual Designer & Artist</div>
        <div class="scroll-indicator">
            <a href="#work" style="color: inherit; text-decoration: none;">
                <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <polyline points="6 9 12 15 18 9"></polyline>
                </svg>
            </a>
        </div>
    </section>

    <!-- Projects Section -->
    <section class="projects" id="work">
        
        <!-- SONGCOVER Section - 6 Squares (1:1) -->
        <div class="project-section fade-in" id="songcover">
            <h2 class="section-title">Songcover</h2>
            <div class="songcover-grid">
                <!-- 🖼️ Row 1 -->
                <div class="project-box">
                    <img src="images/songcover-1.jpg" alt="Songcover 1">
                    <div class="project-info">
                        <h3>PERFEKT</h3>
                    </div>
                </div>
                <div class="project-box">
                    <img src="images/songcover-2.jpg" alt="Songcover 2">
                    <div class="project-info">
                        <h3>TUNNELVISION V1</h3>
                    </div>
                </div>
                <div class="project-box">
                    <img src="images/songcover-3.jpg" alt="Songcover 3">
                    <div class="project-info">
                        <h3>DOG</h3>
                    </div>
                </div>
                
                <!-- 🖼️ Row 2 -->
                <div class="project-box">
                    <img src="images/songcover-4.jpg" alt="Songcover 4">
                    <div class="project-info">
                        <h3>GUTES WETTER</h3>
                    </div>
                </div>
                <div class="project-box">
                    <img src="images/songcover-5.jpg" alt="Songcover 5">
                    <div class="project-info">
                        <h3>TUNNELVISION V2</h3>
                    </div>
                </div>
                <div class="project-box">
                    <img src="images/songcover-6.jpg" alt="Songcover 6">
                    <div class="project-info">
                        <h3>SIGHTSEEING</h3>
                    </div>
                </div>
            </div>
        </div>

        <!-- THUMBNAILS Section - 1 Large (16:9) + 5 Small (16:9) -->
        <div class="project-section fade-in" id="thumbnails">
            <h2 class="section-title">Thumbnails</h2>
            <div class="thumbnails-grid">
                <!-- 🖼️ Large Thumbnail (spans 2 rows on left) -->
                <div class="project-box landscape thumbnail-large">
                    <img src="images/thumbnail-main.jpg" alt="Main Thumbnail">
                    <div class="project-info">
                        <h3>GUERRA</h3>
                    </div>
                </div>
                
                <!-- 🖼️ Small Thumbnails (right side) -->
                <div class="project-box landscape">
                    <img src="images/thumbnail-1.jpg" alt="Thumbnail 1">
                    <div class="project-info">
                        <h3>THRILLER</h3>
                    </div>
                </div>
                <div class="project-box landscape">
                    <img src="images/thumbnail-2.jpg" alt="Thumbnail 2">
                    <div class="project-info">
                        <h3>NONSENSE</h3>
                    </div>
                </div>
                <div class="project-box landscape">
                    <img src="images/thumbnail-3.jpg" alt="Thumbnail 3">
                    <div class="project-info">
                        <h3>TOMAN CITY</h3>
                    </div>
                </div>
                <div class="project-box landscape">
                    <img src="images/thumbnail-4.jpg" alt="Thumbnail 4">
                    <div class="project-info">
                        <h3>TUNNELVISION</h3>
                    </div>
                </div>
                <div class="project-box landscape">
                    <img src="images/thumbnail-5.jpg" alt="Thumbnail 5">
                    <div class="project-info">
                        <h3>STUCK</h3>
                    </div>
                </div>
            </div>
        </div>

        <!-- LOGO DESIGNS Section - 2 Wide Banners (1676x500) -->
        <div class="project-section fade-in" id="logodesigns">
            <h2 class="section-title">Logo Designs</h2>
            <div class="logos-grid">
                <!-- 🖼️ Banner 1 -->
                <div class="project-box banner">
                    <img src="images/logo-banner-1.jpg" alt="Logo Design 1">
                    <div class="project-info">
                        <h3>KUNSTKULLI</h3>
                    </div>
                </div>
                
                <!-- 🖼️ Banner 2 -->
                <div class="project-box banner">
                    <img src="images/logo-banner-2.jpg" alt="Logo Design 2">
                    <div class="project-info">
                        <h3>EP Nußbaum Konzept</h3>
                    </div>
                </div>
            </div>
        </div>

    </section>

    <!-- Lightbox for enlarged images -->
    <div class="lightbox" id="lightbox">
        <div class="lightbox-close" id="lightboxClose">×</div>
        <img src="" alt="Enlarged image" id="lightboxImage">
    </div>

    <!-- Contact Section -->
    <section class="contact" id="contact">
    <p style="margin-bottom: 30px; opacity: 0.5; letter-spacing: 0.15em; font-size: 0.8rem; text-transform: uppercase;">
        Interesse geweckt?
    </p>
    
    <a href="mailto:eric@radfan.de" class="email-btn">
        Schreib mir eine E-Mail
    </a>
</section>

    <!-- JavaScript -->
    <script>
        // ==========================================
        // SCROLL PROGRESS BAR
        // ==========================================
        const scrollProgress = document.querySelector('.scroll-progress');
        
        window.addEventListener('scroll', () => {
            const scrollTop = document.documentElement.scrollTop;
            const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrollPercent = (scrollTop / scrollHeight) * 100;
            scrollProgress.style.width = scrollPercent + '%';
        });

        // ==========================================
        // PARTICLES
        // ==========================================
        const canvas = document.getElementById('particles');
        const ctx = canvas.getContext('2d');
        
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        const particles = [];
        const particleCount = 50;

        class Particle {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.size = Math.random() * 2;
                this.speedX = Math.random() * 0.5 - 0.25;
                this.speedY = Math.random() * 0.5 - 0.25;
                this.opacity = Math.random() * 0.5;
            }

            update() {
                this.x += this.speedX;
                this.y += this.speedY;

                if (this.x > canvas.width) this.x = 0;
                if (this.x < 0) this.x = canvas.width;
                if (this.y > canvas.height) this.y = 0;
                if (this.y < 0) this.y = canvas.height;
            }

            draw() {
                ctx.fillStyle = `rgba(255, 255, 255, ${this.opacity})`;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        for (let i = 0; i < particleCount; i++) {
            particles.push(new Particle());
        }

        function animateParticles() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            particles.forEach(particle => {
                particle.update();
                particle.draw();
            });
            requestAnimationFrame(animateParticles);
        }

        animateParticles();

        window.addEventListener('resize', () => {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        });

        // ==========================================
        // NAVIGATION
        // ==========================================
        const navBtn = document.getElementById('navBtn');
        const navOverlay = document.getElementById('navOverlay');
        const navLinks = navOverlay.querySelectorAll('a');

        navBtn.addEventListener('click', () => {
            navBtn.classList.toggle('active');
            navOverlay.classList.toggle('active');
        });

        navLinks.forEach(link => {
            link.addEventListener('click', () => {
                navBtn.classList.remove('active');
                navOverlay.classList.remove('active');
            });
        });

        // ==========================================
        // LIGHTBOX (Click to enlarge images)
        // ==========================================
        const lightbox = document.getElementById('lightbox');
        const lightboxImage = document.getElementById('lightboxImage');
        const lightboxClose = document.getElementById('lightboxClose');
        const projectBoxes = document.querySelectorAll('.project-box');

        projectBoxes.forEach(box => {
            box.addEventListener('click', () => {
                const img = box.querySelector('img');
                if (img) {
                    lightboxImage.src = img.src;
                    lightbox.classList.add('active');
                }
            });
        });

        // Close lightbox on click
        lightbox.addEventListener('click', () => {
            lightbox.classList.remove('active');
        });

        lightboxClose.addEventListener('click', (e) => {
            e.stopPropagation();
            lightbox.classList.remove('active');
        });

        // Close lightbox with ESC key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && lightbox.classList.contains('active')) {
                lightbox.classList.remove('active');
            }
        });

        // ==========================================
        // INTERSECTION OBSERVER (FADE IN)
        // ==========================================
        const fadeElements = document.querySelectorAll('.fade-in');
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, {
            threshold: 0.1
        });

        fadeElements.forEach(el => observer.observe(el));

        // ==========================================
        // SMOOTH SCROLL
        // ==========================================
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            });
        });
    </script>

</body>
</html>
